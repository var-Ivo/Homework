function multiplicationTable(input) {
    let multiplier = 1;
    let result = 0;
    while (multiplier <= 10) {
        result = input * multiplier;
        console.log(`${input} X ${multiplier} = ${result}`);
        multiplier++;
    }  
}
